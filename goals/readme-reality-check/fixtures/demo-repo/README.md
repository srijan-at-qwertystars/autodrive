# Demo Repo

This fixture intentionally contains stale setup guidance so the auditor has something to catch.

## Quickstart

1. Install dependencies:

   ```bash
   npm install
   npm run dev
   python3 scripts/bootstrap.py
   make setup
   docker compose up
   ```

2. Copy sample environment variables with `cp .env.sample .env`.
